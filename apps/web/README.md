# Web app (`apps/web`)

React + Vite frontend implementing:

- learner authentication (email/password + SSO-ready controls),
- course library tracks for Australian Standards and Main Roads style,
- a three-panel session workspace (source excerpt, AI tutor chat, quiz).
